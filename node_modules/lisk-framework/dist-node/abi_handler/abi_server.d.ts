import { ABI } from '../abi';
import { Logger } from '../logger';
export declare class ABIServer {
    private readonly _socketPath;
    private readonly _router;
    private readonly _logger;
    private readonly _abiHandlers;
    constructor(logger: Logger, socketPath: string, abi: ABI);
    start(): Promise<void>;
    stop(): void;
    private _listenToRequest;
    private _replyError;
}
