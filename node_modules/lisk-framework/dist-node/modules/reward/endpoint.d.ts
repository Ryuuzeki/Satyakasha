import { ModuleEndpointContext } from '../../types';
import { BaseEndpoint } from '../base_endpoint';
import { DefaultReward, InflationRate, ModuleConfig } from './types';
export declare class RewardEndpoint extends BaseEndpoint {
    protected _config: ModuleConfig;
    protected _blockTime: number;
    init(config: ModuleConfig, blockTime: number): void;
    getDefaultRewardAtHeight(context: ModuleEndpointContext): DefaultReward;
    getRewardTokenID(): {
        tokenID: string;
    };
    getAnnualInflation(context: ModuleEndpointContext): InflationRate;
}
