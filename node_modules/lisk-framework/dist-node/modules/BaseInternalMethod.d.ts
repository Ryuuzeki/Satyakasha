import { NamedRegistry } from './named_registry';
export declare abstract class BaseInternalMethod {
    readonly events: NamedRegistry;
    protected readonly stores: NamedRegistry;
    constructor(stores: NamedRegistry, events: NamedRegistry);
}
