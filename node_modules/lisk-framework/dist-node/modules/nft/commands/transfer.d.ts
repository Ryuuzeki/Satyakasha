/// <reference types="node" />
import { CommandExecuteContext, CommandVerifyContext, VerificationResult } from '../../../state_machine';
import { BaseCommand } from '../../base_command';
import { InternalMethod } from '../internal_method';
export interface TransferParams {
    nftID: Buffer;
    recipientAddress: Buffer;
    data: string;
}
export declare class TransferCommand extends BaseCommand {
    schema: {
        $id: string;
        type: string;
        required: string[];
        properties: {
            nftID: {
                dataType: string;
                minLength: number;
                maxLength: number;
                fieldNumber: number;
            };
            recipientAddress: {
                dataType: string;
                format: string;
                fieldNumber: number;
            };
            data: {
                dataType: string;
                minLength: number;
                maxLength: number;
                fieldNumber: number;
            };
        };
    };
    private _internalMethod;
    init(args: {
        internalMethod: InternalMethod;
    }): void;
    verify(context: CommandVerifyContext<TransferParams>): Promise<VerificationResult>;
    execute(context: CommandExecuteContext<TransferParams>): Promise<void>;
}
