import { BaseEndpoint } from '../base_endpoint';
import { ModuleEndpointContext } from '../../types';
import { Validator } from './types';
export declare class PoAEndpoint extends BaseEndpoint {
    private _authorityRegistrationFee;
    init(authorityRegistrationFee: bigint): void;
    getValidator(context: ModuleEndpointContext): Promise<Validator>;
    getAllValidators(context: ModuleEndpointContext): Promise<{
        validators: Validator[];
    }>;
    getRegistrationFee(): {
        fee: string;
    };
}
