import { ModuleEndpointContext } from '../../types';
import { BaseEndpoint } from '../base_endpoint';
export declare class ValidatorsEndpoint extends BaseEndpoint {
    validateBLSKey(ctx: ModuleEndpointContext): Promise<{
        valid: boolean;
    }>;
    getValidator(ctx: ModuleEndpointContext): Promise<{
        generatorKey: string;
        blsKey: string;
    }>;
}
