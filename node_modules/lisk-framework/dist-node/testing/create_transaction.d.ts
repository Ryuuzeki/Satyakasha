/// <reference types="node" />
import { Transaction } from '@liskhq/lisk-chain';
import { CommandClass } from './types';
interface CreateTransactionInput {
    module: string;
    commandClass: CommandClass;
    params: Record<string, unknown> | undefined;
    nonce?: bigint;
    fee?: bigint;
    passphrase?: string;
    chainID?: Buffer;
}
export declare const createTransaction: ({ module, commandClass, params, nonce, fee, passphrase, chainID, }: CreateTransactionInput) => Transaction;
export {};
