import { Network } from './network';
export declare const DEFAULT_RATE_RESET_TIME = 10000;
export declare class BaseNetworkEndpoint {
    protected network: Network;
    private _rateTracker;
    private _limitResetInterval;
    constructor(network: Network);
    start(): void;
    stop(): void;
    protected addRateLimit(procedure: string, peerId: string, limit: number): void;
}
