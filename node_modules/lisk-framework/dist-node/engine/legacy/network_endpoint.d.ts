/// <reference types="node" />
import { Database } from '@liskhq/lisk-db';
import { Logger } from '../../logger';
import { Network } from '../network';
import { BaseNetworkEndpoint } from '../network/base_network_endpoint';
import { Storage } from './storage';
export interface EndpointArgs {
    logger: Logger;
    network: Network;
    db: Database;
}
export declare class LegacyNetworkEndpoint extends BaseNetworkEndpoint {
    readonly _storage: Storage;
    private readonly _logger;
    private readonly _network;
    constructor(args: EndpointArgs);
    handleRPCGetLegacyBlocksFromID(data: unknown, peerId: string): Promise<Buffer>;
}
